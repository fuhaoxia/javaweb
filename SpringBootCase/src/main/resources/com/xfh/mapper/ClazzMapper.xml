<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xfh.mapper.ClazzMapper">
    
    <select id="findAllClazzByConditions" resultType="com.xfh.domain.Clazz">
    select c.*,e.name master_name from clazz c left join emp e on c.master_id=e.id
    <where>
        <if test="name !=null and name !=''">name=#{name} </if>
        <if test="begin!=null ">and begin_date between #{begin} and #{end} </if>
        <if test="end !=null">and end_date between #{begin} and #{end} </if>
    </where>

    </select>
    
    <update id="updateClazz" >
        update clazz set name=#{name},room=#{room},begin_date=#{beginDate},end_date=#{endDate},
                         master_id=#{masterId},subject=#{subject} where id=#{id}
    </update>


    
</mapper>